# AGENTS.md

Future Codex sessions must preserve these project rules:

- Never soften MB rules.
- Never skip Magic Check.
- Never allow AI override.
- Always run tests after changing rule logic.
- Preserve deterministic execution.
- Preserve audit logging.
- Magic Check is final and non-bypassable.
- Every rejected pick must expose the exact rejection reason.
- Every approved pick must expose an audit trail.

